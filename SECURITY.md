# 安全说明

## 适用范围

3IN1 是进入 `com.android.systemui` 的 Root/LSPosed 模块。错误的适配、作用域或框架状态可能导致 SystemUI 异常、状态栏回退或设备需要进入安全恢复流程。

请只在已知适配 gate 上测试，并将 LSPosed 作用域限制为 `com.android.systemui`。遇到异常时，优先在 LSPosed 停用 3IN1，再重启 SystemUI 或设备。

## 报告安全问题

当前仓库尚未配置专用安全邮箱或私密漏洞接收地址。请不要在公开 Issue 中发布可直接利用的 Root、LSPosed 注入、权限绕过或设备识别信息。先联系项目维护者，并提供最小化、已脱敏的复现说明；后续由维护者补充正式安全联系方式。

报告中请包含：受影响版本、必要的环境版本、影响范围、最小复现步骤、恢复方式，以及是否已公开或被利用。不要附带签名密钥、设备 serial/UID、账户信息、完整原始日志、系统 APK 或反编译内容。

## 隐私与数据

应用不提供远程遥测。诊断信息用于本机设置页与 SystemUI 回执；项目公开文档不应包含设备标识或原始日志。发布 APK 时不得公开 debug/test 签名密钥。

## 许可证状态

项目许可证暂未指定；安全修复和贡献处理不改变这一状态。
